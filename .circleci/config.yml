version: 2.1
            
jobs:
  build-frontend:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: cd frontend
      - run: ls 
      - run: npm i
      - run: npm run lint
  # build-backend:
  #   docker:
  #     - image: circleci/node:13.8.0
  #   steps:
  #     - checkout
  #     - run: cd backend
  #     - run: npm i
  #     - save_cache:
  #         paths: [backend/node_modules]
  #         key: "backend-build-npm-packages"
  #     - run: npm run build

  # test-frontend:
  #   docker:
  #     - image: 
  #   steps:
  #     - checkout
  #     - restore_cache:
  #         keys:
  #           - "frontend-build-npm-packages"
  #     - run: npm i
  #     - run: npm run test  
      
                
  # test-backend:
  #   docker:
  #     # Docker image here
  #   steps:
  #     # Checkout code from git
  #     # Restore from cache
  #     # Your job code here
            
  # scan-frontend:
  #   docker:
  #     # Docker image here
  #   steps:
  #     - checkout
  #     - restore_cache:
  #         keys:
  #           - "frontend-build-npm-packages"
  #     - run: npm audit
  # scan-backend:
  #   docker:
  #     # Docker image here
  #   steps:
  #     # Checkout code from git
  #     # Restore from cache
  #     # Your job code here

  
workflows:
  default:
    jobs:
      - build-frontend
      # - build-backend
      # - test-frontend:
      #     requires: [build-frontend]
      # - test-backend:
      #     requires: [build-backend]
      # - scan-backend:
      #     requires: [build-backend]
      # - scan-frontend:
      #     requires: [build-frontend]
      